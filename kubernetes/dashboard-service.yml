apiVersion: v1
kind: Service
metadata:
  name: tvs-dashboard-svc
  namespace: traffic
spec:
  selector:
    app: tvs-dashboard
  ports:
  - port: 8501
    targetPort: 8501
  type: LoadBalancer      # exposes externally — gives you public IP

apiVersion: v1
kind: Service
metadata:
  name: tvs-backend-svc
  namespace: traffic
spec:
  selector:
    app: tvs-backend
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP        # internal only, dashboard talks to this
